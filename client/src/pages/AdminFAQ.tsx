import DashboardLayout from "@/components/DashboardLayout";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Textarea } from "@/components/ui/textarea";
import { trpc } from "@/lib/trpc";
import { Check, Pencil, Plus, Save, Trash2, X } from "lucide-react";
import { useState } from "react";

type FaqForm = { id?: number; question: string; answer: string; status: "draft" | "published"; sortOrder: number };
const emptyForm: FaqForm = { question: "", answer: "", status: "published", sortOrder: 0 };

export default function AdminFAQ() {
  const faqs = trpc.faq.adminList.useQuery();
  const utils = trpc.useUtils();
  const [form, setForm] = useState<FaqForm | null>(null);
  const create = trpc.faq.create.useMutation({ onSuccess: async () => { setForm(null); await utils.faq.adminList.invalidate(); await utils.faq.published.invalidate(); } });
  const update = trpc.faq.update.useMutation({ onSuccess: async () => { setForm(null); await utils.faq.adminList.invalidate(); await utils.faq.published.invalidate(); } });
  const remove = trpc.faq.remove.useMutation({ onSuccess: () => void utils.faq.adminList.invalidate() });
  const save = (event: React.FormEvent) => { event.preventDefault(); if (!form) return; if (form.id !== undefined) update.mutate({ id: form.id, question: form.question, answer: form.answer, status: form.status, sortOrder: form.sortOrder }); else create.mutate({ question: form.question, answer: form.answer, status: form.status, sortOrder: form.sortOrder }); };

  return <DashboardLayout><div className="admin-projects-page"><header className="admin-page-header"><div><p className="eyebrow">Perla Marine · Yönetim</p><h1>Sık Sorulan Sorular</h1><p>Ana sayfada ve SSS sayfasında gösterilecek soruları, cevapları ve yayın sırasını yönetin.</p></div><Button onClick={() => setForm(emptyForm)}><Plus size={17} /> Yeni soru</Button></header>{form && <form className="admin-project-form" onSubmit={save}><div className="admin-project-form__header"><div><p className="eyebrow">SSS içeriği</p><h2>{form.id ? "Soruyu düzenle" : "Yeni soru ekle"}</h2></div><button type="button" className="admin-icon-button" onClick={() => setForm(null)} aria-label="Formu kapat"><X size={18} /></button></div><div className="admin-project-form__grid"><label className="admin-project-form__full">Soru<Input required minLength={10} maxLength={320} value={form.question} onChange={(event) => setForm({ ...form, question: event.target.value })} /></label><label className="admin-project-form__full">Cevap<Textarea required minLength={20} maxLength={4000} rows={5} value={form.answer} onChange={(event) => setForm({ ...form, answer: event.target.value })} /></label><label>Durum<select value={form.status} onChange={(event) => setForm({ ...form, status: event.target.value as FaqForm["status"] })}><option value="published">Yayında</option><option value="draft">Taslak</option></select></label><label>Sıra<Input type="number" min={0} value={form.sortOrder} onChange={(event) => setForm({ ...form, sortOrder: Number(event.target.value) })} /></label></div>{(create.error || update.error) && <p className="admin-form-error" role="alert">SSS kaydedilemedi. Alanları kontrol edip tekrar deneyin.</p>}<div className="admin-project-form__actions"><Button type="button" variant="outline" onClick={() => setForm(null)}>Vazgeç</Button><Button type="submit" disabled={create.isPending || update.isPending}><Save size={16} /> {create.isPending || update.isPending ? "Kaydediliyor…" : "Kaydet"}</Button></div></form>}<section className="admin-project-list" aria-label="SSS listesi"><div className="admin-project-list__header"><h2>Tüm sorular</h2><span>{faqs.data?.length ?? 0} kayıt</span></div>{faqs.isLoading ? <p className="admin-empty">Sorular yükleniyor…</p> : faqs.error ? <p className="admin-form-error" role="alert">SSS listesi yüklenemedi.</p> : faqs.data?.length ? faqs.data.map((faq) => <article className="admin-project-row" key={faq.id}><div className="admin-project-row__copy"><div className="admin-project-row__meta"><span>Sıra {faq.sortOrder}</span><strong className={`admin-status admin-status--${faq.status}`}>{faq.status === "published" ? <><Check size={13} /> Yayında</> : "Taslak"}</strong></div><h3>{faq.question}</h3><p>{faq.answer}</p></div><div className="admin-project-row__actions"><Button variant="outline" size="sm" onClick={() => setForm(faq)}><Pencil size={15} /> Düzenle</Button><Button variant="ghost" size="sm" className="admin-delete-button" onClick={() => { if (window.confirm("Bu soruyu kaldırmak istediğinize emin misiniz?")) remove.mutate({ id: faq.id }); }}><Trash2 size={15} /> Sil</Button></div></article>) : <div className="admin-empty"><p>Henüz SSS içeriği yok.</p><Button onClick={() => setForm(emptyForm)}><Plus size={16} /> İlk soruyu ekle</Button></div>}</section></div></DashboardLayout>;
}
